apiVersion: v1
kind: Service
metadata:
  name: project2-infra-service
spec:
  selector:
    app: project2-infra
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort